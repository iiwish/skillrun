task_id: T033
schema_version: "1.0"
packet_id: "v0.4-T033"
feature_id: "v0.4"
title: Convert Runtime Dependency Failures To DependencyError
status: Draft
priority: P0
source_spec: ".ai-platform/specs/v0.4/spec.md"
source_plan: ".ai-platform/specs/v0.4/plan.md"
source_tasks: ".ai-platform/specs/v0.4/tasks.md"
created: 2026-05-13
governance_inputs:
  constitution: ".ai-platform/memory/constitution.md"
  spec: ".ai-platform/specs/v0.4/spec.md"
  plan: ".ai-platform/specs/v0.4/plan.md"
  tasks: ".ai-platform/specs/v0.4/tasks.md"
  checklist: ".ai-platform/specs/v0.4/checklists/requirements.md"
  analysis: ".ai-platform/specs/v0.4/analysis.md"
adapter: codex
work_unit:
  task_id: T033
  title: Convert Runtime Dependency Failures To DependencyError
  status: Draft
  feature_id: v0.4
goal: Ensure skillrun test and skillrun run return structured DependencyError envelopes for dependency failures.
requirement_mapping:
  - FR-005
  - NFR-004
dependencies:
  - T032
blocks:
  - T034
  - T036
allowed_files:
  - src/runtime.rs
  - src/errors.rs
  - src/adapters/python.rs
  - src/adapters/node.rs
  - src/readiness.rs
  - tests/runtime.rs
  - tests/errors.rs
conflicts_with:
  - T029
  - T032
  - T034
must_not_change:
  - src/mcp.rs
  - src/pack.rs
  - tests/mcp_server.rs
codebase_context:
  - "Runtime currently validates Manifest before dispatching to adapters."
  - "Stale Manifest must fail before dependency probing."
  - "Existing ValidationError, PolicyViolation, ProtocolViolation and RuntimeError behavior must remain stable."
execution_constraints:
  - "Do not change MCP behavior in this task."
  - "Do not weaken stale Manifest guards."
  - "Do not expose stack traces in display.markdown."
tdd_plan:
  red: "Add failing runtime tests for missing executable/package dependency returning DependencyError."
  green: "Wire readiness failures into runtime envelopes."
  refactor: "Centralize dependency error construction if duplication appears."
validation_loop:
  failure_policy: "If dependency simulation breaks unrelated tests, isolate the simulation helper rather than weakening runtime assertions."
  commands_source: validation_commands
validation_commands:
  - "cargo test --test runtime --test errors"
  - "cargo test"
review_contract:
  spec_compliance_review: required
  engineering_quality_review: required
  qa_acceptance_review: required
  user_acceptance_required: true
evidence_contract:
  summary: ".ai-platform/evidence/T033/summary.md"
  test_results: ".ai-platform/evidence/T033/test-results.md"
  diff: ".ai-platform/evidence/T033/diff.patch"
acceptance_criteria:
  - "Missing Python, missing Node and missing Pydantic produce DependencyError."
  - "Stale Manifest still fails before dependency probing."
  - "Existing structured error behavior remains stable."
definition_of_done:
  - "Target tests pass."
  - "Full cargo test passes."
handoff:
  evidence_dir: ".ai-platform/evidence/T033"
  next_status_after_execution: Needs_Review
  commit_policy: "Commit only after review passes and user requests git handoff."
stop_conditions:
  - "MCP changes are required; defer them to T034."
  - "Fix requires automatic dependency installation or vendoring."
