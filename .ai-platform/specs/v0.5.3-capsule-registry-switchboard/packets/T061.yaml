schema_version: "1.0"
packet_id: "v0.5.3-T061-A001"
task_id: "T061"
feature_id: "v0.5.3-capsule-registry-switchboard"
title: "Finalize v0.5.3 docs and release evidence"
status: "Ready_For_Execution"
created_at: "2026-05-15"
execution_mode: "direct"

governance:
  source_spec: ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/spec.md"
  source_plan: ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/plan.md"
  source_tasks: ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/tasks.md"
  source_analysis: ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/analysis.md"
  prerequisite: "T059 and T060 completed with cargo test passing."
  constraints:
    - "Do not introduce new runtime behavior."
    - "Docs must state registry is local inventory, not marketplace or trust."
    - "Docs must state enabled means future exposure intent, not sandbox/trust."
    - "Docs must state v0.5.3 does not include Router or MCP client mount profiles."

scope:
  goal: "Align public docs, release notes, and governance evidence with implemented local registry and switchboard behavior."
  allowed_files:
    - "README.md"
    - "README.zh-CN.md"
    - "docs/README.md"
    - "docs/v0.5.3-capsule-registry-switchboard.md"
    - "RELEASE_NOTES.md"
    - ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/analysis.md"
    - ".ai-platform/specs/v0.5.3-capsule-registry-switchboard/tasks.md"

acceptance_criteria:
  - "Docs mention registry and switchboard commands."
  - "Docs explicitly keep Router, MCP mount, .skr import, marketplace, trust, sandbox, and dependency install out of scope."
  - "Release notes describe v0.5.3 as local inventory and exposure intent."
  - "Final validation includes git diff --check and cargo test."

validation:
  required_commands:
    - "git diff --check"
    - "cargo test"
  evidence_required:
    - "Changed files"
    - "Validation results"
    - "Diff summary"
    - "Residual risk"
